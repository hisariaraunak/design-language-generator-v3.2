# Browsers Adapter

Browser rendering, snapshots, accessibility and responsive verification.

Adapters are trusted, capability-scoped integrations and may not execute arbitrary commands from project data.
