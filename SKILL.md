# Design Language Generator v3.2.0

Use `spec/` as the only authoritative definition of phases, gates, artifacts and requirements. Runtime behavior must consume that specification rather than duplicate it in prose. Human intervention remains limited to Gate A and Gate B for the reference workflow.
