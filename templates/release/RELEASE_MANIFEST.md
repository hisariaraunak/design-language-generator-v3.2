# Release Manifest

The machine-readable `manifest.json` is authoritative. It contains generator version, design-system version, release status, completion states, gate approvals and every included artifact with SHA-256 checksum and byte size. Production is blocked on stale artifacts, failed or unverified mandatory evidence, missing files or checksum drift.
