# Implementation Mapping

Map every semantic token and component ID to each supported platform. For each mapping specify source specification, generated implementation path, API name, state mapping, accessibility behavior, theme behavior and divergence classification: `must_match`, `platform_native` or `allowed_divergence`. Any unmapped required component blocks implementation completeness.
