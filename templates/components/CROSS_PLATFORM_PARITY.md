# Cross-Platform Parity

Classify each behavior as `must_match`, `platform_native` or `allowed_divergence`. Define visual and behavioral tolerances, token equivalence, component-state equivalence, accessibility expectations, native navigation exceptions and evidence used to approve each divergence. Undeclared divergence is a validation failure.
