# Compatibility Matrix

Record each platform/runtime, minimum supported version, implementation package version, theme support, locale/RTL support, accessibility support, snapshot baseline revision, known allowed divergences and last verified evidence set. Any unsupported combination must be explicitly marked rather than silently omitted.
