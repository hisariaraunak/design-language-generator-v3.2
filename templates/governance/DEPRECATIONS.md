# Deprecations

For every deprecated token/component/API record: stable ID, replacement, deprecated-in version, target removal version, migration instructions, usage count, owners and evidence that supported migrations work. Removal is blocked while the compatibility policy says the deprecated surface is still supported.
